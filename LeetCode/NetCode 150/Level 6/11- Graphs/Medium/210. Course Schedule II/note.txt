The used Data Structure:
  Graph.
  Time And Space Complexity:
    Time Complexity value is O(v + e).
    Space Complexity value is O(v + e).
      `v` is the courses number -Graph vertices-.
      `e` is the prerequisites number -Graph edge-.